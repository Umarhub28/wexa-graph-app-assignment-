spring.application.name=devmatch

# Render provides PORT for the public HTTP service.
server.port=${PORT:8080}

# Neo4j / CognoDB Connection Properties
neo4j.uri=${NEO4J_URI}
neo4j.username=${NEO4J_USERNAME}
neo4j.password=${NEO4J_PASSWORD}
